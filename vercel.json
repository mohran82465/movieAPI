{
  "version": 2,
  "build": [
    
    {"env": {"NODE_ENV": "production"}}
],
  "routes": [
    {
      "src": "/{.*}",
      "dest": "server.js"
    }
  ],
  "builds": [{ "src": "server.js", "use": "@vercel/node" }]
}
